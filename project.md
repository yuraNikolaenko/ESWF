## ESWF (ERP System Web Framework)

**ESWF (ERP System Web Framework)** — це шаблон для розробки ERP-систем будь-якого напрямку: автотранспорт, логістика, дистрибуція, виробництво, облік тощо. Поточна реалізація з модулем "Транспорт" використовується як демонстраційний приклад для тестування архітектури, UI, аналітики й структури додатку. Модуль "Бухгалтерія" — основа фреймворку, оскільки всі системи потребують базових облікових довідників і документів.

---

## 📦 Загальні відомості

* **Назва проєкту:** ESWF (ERP System Web Framework)
* **Призначення:** шаблон-фреймворк для швидкої розробки ERP-систем різного типу. Поточна реалізація демонструє підсистему "Управління автотранспортом" як приклад функціонального модуля.
* **Архітектура:**

  * Frontend: React + Ant Design

    > 🧠 **Обґрунтування вибору стеку:**
    >
    > React — це найпопулярніша бібліотека для побудови UI, що забезпечує масштабованість, гнучкість, потужну екосистему (React Router, Context API, хуки), а також легко інтегрується з backend через REST API.
    >
    > Ant Design — UI-фреймворк, оптимізований для **бізнес-застосунків**, що надає сотні готових компонентів таблиць, форм, панелей і діалогів. Це особливо важливо для ERP-систем, які оперують великою кількістю однотипних інтерфейсів.
    >
    > 📌 Попередньо розглядались також: Material UI (занадто загальний), PrimeReact (не вистачає стабільності), Bootstrap (застарілий підхід). Ant Design дає швидкий старт, але має недоліки при ускладненні форм — про це буде окремий висновок після створення перших 5–10 документів.

    ## 🚫 Відомі обмеження Ant Design у великих ERP

> Попри численні переваги, при зростанні складності форми та кількості одночасно відкритих компонентів Ant Design може викликати проблеми:
>
> * 🐢 **Продуктивність:** при великій кількості полів, динамічних вкладках та умовних правилах валідації — форми починають "підгальмовувати".
> * 🧱 **Фіксований підхід до форм:** важко реалізовувати кастомні складні валідації, комбіновані блоки, або залежні поля.
> * 🌀 **useForm і context:** взаємодія з Form.Provider не завжди стабільна при вкладеній структурі.
>
> 📌 Це не означає, що AntD не підходить. Але з досвіду — після розробки першого серйозного документа (наприклад, путевого листа або рахунку) потрібно буде:
>
> * або глибоко кастомізувати компоненти AntD
> * або поступово замінювати форму на більш гнучкі рішення (наприклад, react-hook-form + custom layout)

    > 🧩 **Компоненти форми залежать від типу метаданих:**
    >
    > * `MasterDataList`, `MasterDataForm` — для довідників (master data)
    > * `TransactionList`, `TransactionForm` — для документів (transactional data)
    > * `ReportViewer` — для звітів
    > * `RegisterTable` — для реєстрів (ledgers, journals)
    > * `CustomTool` або `ProcessPanel` — для сервісів і процесів
    >
    > Компоненти форм адаптовані до типу даних та підтримують вкладки, статуси, автозбереження тощо.

    * Структура папок:

      * `src/components` — реюзовані компоненти інтерфейсу
      * `src/pages` — сторінки (довідники, документи)
      * `src/context` — контексти React (наприклад, `TabContext`)
      * `src/hooks` — власні хуки (useTabs, useFetch тощо)
      * `src/config` — глобальні константи, іконки, маппінги
      * `src/styles` — CSS/SCSS/Tailwind/DaisyUI стилі
      * `src/utils` — допоміжні функції (форматування, API-хелпери)
      * `public` — статичні файли
      * `Dockerfile`, `vite.config.js` — інфраструктура
  * Backend: Django + Django REST Framework

    * Структура:

      * `backend/` — Django-проєкт
      * `core/` — додаток ядра з API, models, views, permissions
      * `init_db.py`, `init_db_runner.py` — скрипти ініціалізації
      * `start.sh` — запуск сервера
      * `.env`, `requirements.txt`, `db.sqlite3` — середовище
  * База даних: SQLite (із планом переходу на PostgreSQL)
  * Інфраструктура: `docker-compose.yml` + `docker-compose.debug.yml` для запуску в Docker
  * Файл документації: `project.md` у корені проєкту

  > 💡 **Пропозиція щодо реорганізації структури проекту:**
>
> **Backend:**
>
> * Розбити `core/` на окремі додатки (apps):
>
>   * `essentials/` — бухгалтерія
>   * `fleet/` — логістика, автопарк
>   * `chatgpt/` — інтеграція з OpenAI
>   * `core/` — залишити для базових налаштувань, auth, utils
>
> **Frontend:**
>
> * Створити папку `src/api/` для розділення fetch/axios-запитів:
>
>   * `vehicleApi.js`, `driverApi.js`, `chatApi.js`, `essentialsApi.js`
> * Всередині `src/pages/` створити вкладені структури:
>
>   * `pages/vehicles/`, `pages/waybills/`, `pages/invoices/`
>
> **Документація:**
>
> * Розбити `project.md` на тематичні файли:
>
>   * `architecture.md`, `data-models.md`, `api-specs.md`, `dev-notes.md`
>
> Це забезпечить масштабованість та зручність у підтримці коду.

### Структура інтерфейсу:

* Header (верхня панель)
* Main Area:

  * Sidebar (зліва) — розділи
  * ContentArea (по центру) — форми/довідники
  * ChatPanel (справа) — чат-панель з GPT
* FooterTabs — вкладки внизу

---

## 📱 Мобільний застосунок водія: план інтеграції

### 🎯 Мета:

Забезпечити водіїв мобільним інтерфейсом для перегляду рейсів, підтвердження замовлень, фіксації виконання поїздок, контролю пального й маршрутів у реальному часі.

### 📱 Можливі варіанти реалізації:

* **Flutter (Dart)** — кросплатформенна розробка (Android + iOS з єдиним кодом)
* **React Native** — альтернатива з ближчою інтеграцією з існуючим React стеком

### 🔌 API-звʼязок:

* Отримання списку рейсів
* Деталі замовлення: маршрут, пункт призначення, вантаж
* Фіксація статусу: "Вирушив", "На місці", "Доставлено"
* Передача фото / підпису (опціонально)

### 🔐 Безпека:

* Авторизація по JWT або токену
* Водій бачить лише свої поїздки

### 📋 Початковий план:

1. **API:** Створити окрему зону `/api/mobile/` із доступом до маршрутів і замовлень
2. **Модель:** Додати зв’язок між водієм та активним рейсом
3. **Фронтенд:** Створити базовий застосунок на Flutter з екраном "Список замовлень"
4. **Валідація:** Перевірка статусів, часу, координат GPS (опціонально)
5. **Тестування:** Внутрішній запуск з 1–2 водіями

> 💡 Надалі можливе розширення функціоналу: карта, push-нотифікації, зв'язок із диспетчером

---

## 🧩 Backend: реалізовані довідники та API

### ✅ Статус реалізації:

* Створено моделі довідників **Автомобілі** та **Водії** з усіма ключовими полями (включно з зовнішніми ключами, статусами, пробігом, типом пального тощо).
* Реалізовано повноцінні API-endpoint-и через **Django REST Framework**:

  * `GET /api/vehicles/` — список автомобілів
  * `GET /api/drivers/` — список водіїв
  * `POST /api/vehicles/`, `PUT`, `DELETE` — створення, редагування, видалення
  * Аналогічні запити для `drivers`
* Підтримується пагінація, фільтрація та сортування
* Усі API інтегруються з frontend через axios-запити (в планах — винесення в `src/api/`)

### 🔐 Безпека:

* Впроваджено базову перевірку прав (read/write)
* Впроваджується рольова модель доступу через JWT і Django Permissions

### 📦 Структура моделей:

* `Vehicle` — має поля: `plate`, `brand`, `model`, `year`, `vin`, `mileage`, `fuel_type`, `status`, `driver`
* `Driver` — `name`, `birth_year`, `gender`, `ipn`

> 💡 Планується винесення обох моделей у додаток `fleet/`, окремо від `core/`

---

## 📃 Essentials — базовий модуль шаблону

### 📘 Призначення:

Модуль "Essentials" — це фундаментальний компонент ESWF, який забезпечує облік фінансів, роботу з рахунками, документами, валютою, організаційною структурою підприємства.

### 📚 Структура:

#### Довідники:

* Організації (Organizations)
* Валюти (Currencies)
* Клієнти / Контрагенти (Clients)
* План рахунків (Chart of Accounts)
* Господарські операції (Business Operations)
* Контракти

#### Операції:

* Рахунки-фактури (Invoices)
* Надходження платежів (Incoming Payments)
* Документ-операції (Document Operations)

#### Аналітика:

* Баланс
* Звіт про прибутки і збитки
* Рух грошових коштів (Cash Flow)

### 💡 Особливості:

* Довідник рахунків підтримує ієрархічну структуру
* Повна інтеграція з реєстрами: рухи автоматично відображаються у журналах і леджерах
* Підтримка мультивалютності та декількох організацій
* Єдина база для зв'язку з іншими модулями: документи з транспорту, закупівель чи складу впливають на бухгалтерські записи

---

## 📂 Основні модулі

Усі модулі системи класифікуються за типом метаданих відповідно до підходів побудови ERP:

### 📚 Master Data (Довідники)

> Дані, що описують об'єкти системи й використовуються у документах та операціях:

* Автомобілі (Vehicles)
* Водії (Drivers)
* Маршрути (Routes)
* Пункти призначення (Destinations)
* Номенклатура (Items)
* Контрагенти (Clients)
* Організації (Organizations)
* Валюти (Currencies)
* Склади (Warehouses)
* План рахунків (Chart of Accounts)
* Господарські операції (Business Operations)
* Контракти (Contracts)

### 🧾 Transactional Data (Документи, операції)

> Дані, що фіксують факти господарської діяльності, рухи та взаємодії:

* Путеві листи (Waybills)
* Ремонтні відомості (Maintenance Records)
* Рахунки-фактури (Invoices)
* Надходження платежів (Incoming Payments)
* Документ-операції (Document Operations)

### 📊 Analytical Data (Звіти)

> Зведена інформація для контролю, аналізу та планування:

* Звіт про рух пального (Fuel Movement Report)
* Статистика транспорту (Fleet Statistics)
* Звіт про прибутки та збитки (P\&L)
* Рух грошових коштів (Cash Flow)
* Баланс (Balance Sheet)

### 🧮 Registers (Журнали й залишки)

> Агреговані підсумки та хронологічні записи для бухгалтерського й оперативного обліку:

* Грошовий леджер (Cash Ledger)
* Леджер запасів (Inventory Ledger)
* Грошовий журнал (Cash Journal)
* Журнал товарів (Inventory Journal)

### ⚙️ Service & Processes (Процеси та налаштування)

> Інструменти обслуговування та автоматизації:

* Обмін з BAF (Exchange with BAF)
* Видалення об'єктів (Delete Marked Objects)
* Налаштування застосунку (Application Settings)
* Канбан-дошка (Kanban Board)

---

## 📁 Структура довідника "Пункти призначення"

### 📍 `location_point`

* Населені пункти України та інших країн
* Поля: id, name, region, country, latitude, longitude

### 🏗 `transport_hub`

* Транспортні вузли (порти, аеропорти, станції)
* Поля: id, name, type, location\_point\_id (FK), code, latitude, longitude

### 📦 `delivery_point`

* Адреси доставки (неточні або точні координати)
* Поля: id, location\_point\_id (FK), address, note, latitude, longitude

### 🔌 API (поточна логіка доступу)

* GET `/api/location-points` — всі населені пункти
* GET `/api/transport-hubs?location_id=1` — вузли міста
* GET `/api/delivery-points?location_id=1` — адреси доставки в місті

### 🨓 Принципи географії

* Вся географічна логіка обʼєднується в абстракцію `destination`
* Один населений пункт може мати багато хабів і багато адрес доставки
* Імпорт довідників можливий з відкритих джерел (OSM, GeoNames, КОАТУУ)

---

## 🤖 Чат підприємства (AI Chat)

### 📥 Реалізація:

* Фронтенд: `ChatPanel.jsx`
* Бекенд: Django endpoint `/api/chat/`
* GPT: системний prompt з логістичним контекстом

### 🨠 Мета:

* Відповіді на запити типу: *“Яка собівартість перевезення 8 т піску з Києва до Одеси?”*
* Відповіді базуються на ERP-даних

### 💡 Технічні деталі:

* Використовується `openai==0.28`
* Ключ зберігається в `.env` + dotenv
* Запити: `POST /api/chat/` з `message`

---

## ✅ Поточна реалізація GPT (травень 2025)

* Endpoint `/api/chat/` (Django)
* `semantic_parser.py` — аналіз intent користувача
* `normalize_text()` — очищення тексту
* Обробка запитів:

  * `count_all`, `get_field_by_name`, `vehicle_age_query`, `driver_age_query`
* Працює з моделями `Driver`, `Vehicle`
* Прості запити обробляються без GPT
* Prompt semantic parser підтримує синоніми, орфографію, суржик

### 📌 Статус:

* ✅ Базовий чат функціонує
* 🟡 Наступні кроки:

  1. Зберігання історії
  2. Передача внутрішніх даних ERP у prompt
  3. Обчислення собівартості
  4. Ролі й авторизація
  5. Підтримка реальних довідників

---

## 🤖 Очікування від AI

### 🏆 Мета:

* Створити логістичного помічника, який:

  * Розуміє помилки, суржик, плутанину мов
  * Має доступ до ERP
  * Виконує аналітику типу: "Варто відправляти машину в Доманівку?"
  * Комбінує інформацію з довідників, маршрутів, замовлень
  * Запитує додаткові дані (function calling)

### 🚀 Очікуваний функціонал:

* Доступ до замовлень, маршрутів, водіїв, машин
* Function calling до API
* Агентна логіка: комбінування запитів, прийняття рішень
* Аналіз відстаней: внутрішній довідник або API

### 🛠 Методи реалізації:

* Початково: ключові слова → ORM → GPT
* Далі: function calling
* Ще далі: агент з логікою


###  Облік залишків і рухів

📒 Таблиця `cash_journal` – журнал рухів коштів
•	- `id` — PK
•	- `organization_id` — FK → organizations
•	- `account_id` — FK → chart_of_accounts
•	- `currency_id` — FK → currencies
•	- `movement_type` — 'in' / 'out'
•	- `amount` — сума операції
•	- `document_type` — тип документа (наприклад, incomingPayments)
•	- `document_id` — ID документа
•	- `movement_date` — дата операції
•	- `created_at` — створено
•	- `comment` — примітка

📘 Таблиця `cash_ledger` – залишки по рахунках
•	- `id` — PK
•	- `organization_id` — FK → organizations
•	- `account_id` — FK → chart_of_accounts
•	- `currency_id` — FK → currencies
•	- `balance` — поточний залишок
•	- `last_update` — дата останнього оновлення

🧠 Механізм
•	- Кожна операція додається до `cash_journal`
•	- Одночасно оновлюється `cash_ledger`
•	- Швидке отримання залишку: `SELECT balance FROM cash_ledger WHERE account_id = ...`


# 🧾 Стан реалізації проєкту (на 2025-05-03)

## 🧱 Загальна структура:
- Проєкт `eswf` на Django
- Один додаток: `core`
- SQLite як база (тимчасово)
- Фікстури, генерація, імпорт даних

---

## 📦 Моделі:

### 🔹 Country
- Імпортований із JSON (`countries_merged.json`)
- Поля: `name`, `name_local`, `alpha2_code`, `alpha3_code`, `numeric_code`
- Службові поля: `uuid`, `ismark`

### 🔹 LocationPoint
- Імпорт з `locationpoints_with_code.json`
- Ієрархія вручну через `parent` (без MPTT)
- Визначення parent на основі КОАТУУ коду
- Прив'язка до Country

### 🔹 Driver
- Генерація: `generate_drivers.py`
- Поля: ПІБ, стать, ІПН, email, ліцензія, телефон, адреса, фото
- Службові: `uuid`, `isfolder`, `ismark`, `parent`

### 🔹 Vehicle
- Генерація: `generate_vehicles.py`
- Прив’язка до водія
- Поля: бренд, модель, VIN, номер, рік, фото
- Службові: `uuid`, `isfolder`, `ismark`, `parent`

### 🔹 TransportHub та DeliveryPoint
- Прив’язані до LocationPoint
- Поля: тип, координати, коментар
- Службові: `uuid`, `isfolder`, `ismark`, `parent`

### 🔹 Route
- Нова модель
- Поля: `parent`, `ismark`, `isfolder`, `comment`
- Прив’язка: `departure_point`, `return_point` (до LocationPoint)

---

## 🛠️ Скрипти та команди:

### 📂 `core/management/commands/`
- `load_initial_data.py` — головний імпортер:
    - імпорт країн
    - імпорт населених пунктів
    - запуск `assign_parents`
    - запуск `generate_drivers`
    - запуск `generate_vehicles`
- `import_locationpoints.py`
- `assign_parents.py`
- `generate_drivers.py`
- `generate_vehicles.py`

### 📂 `core/fixtures/initial/`
- `countries_merged.json`
- `locationpoints_with_code.json`

---

## ⚙️ Інше:
- Дані не зберігаються в корені — використовуються `fixtures/initial`
- Ураховано кодування BOM (`utf-8-sig`)
- MPTT скасовано — використовується явна ієрархія
- Усі моделі підтримують: `uuid`, `isfolder`, `ismark`, `parent`
- Панель адміністратора адаптована, додано Route

## 🧩 Backend: структура та логіка універсального API (станом на травень 2025)

Після повної реорганізації та "обнулення" backend, реалізовано **універсальний REST API** для довідників на базі `Django REST Framework`, з мінімальним кодом і повною масштабованістю.

---

### 📂 Структура `core/api/masterdata.py`

| Компонент | Призначення |
|----------|-------------|
| `MasterdataMeta` | `GET /api/<code>/meta/` – повертає метадані (поля, типи, назви) |
| `MasterdataData` | `GET /api/<code>/` – повертає лише список обʼєктів |
| `MasterdataFull` | `GET /api/<code>/full/` – повертає одночасно `meta + data` |
| `MasterdataItem` | `GET /api/<code>/<id>/` – повертає окремий обʼєкт для редагування |

---

### 📄 Формат відповіді `meta`

```json
{
  "fields": [
    {
      "name": "country_id",
      "type": "reference",
      "title": {"ua": "Країна", "en": "Country"},
      "ref": "country"
    },
    ...
  ]
}
```

Поля формуються динамічно через introspection `model._meta.fields`, а локалізовані назви підтягуються з `field_titles.py`.

---

### 🔁 `urls.py`

```python
urlpatterns = [
  path('<str:code>/meta/', MasterdataMeta.as_view()),
  path('<str:code>/full/', MasterdataFull.as_view()),
  path('<str:code>/<int:id>/', MasterdataItem.as_view()),
  path('<str:code>/', MasterdataData.as_view()),
]
```

---

### 🛠 Як це працює

#### 📥 Отримати дані для таблиці
```http
GET /api/drivers/full/
```
- Повертає: `{ meta, data }`

#### 📥 Отримати обʼєкт для редагування
```http
GET /api/drivers/12/
```
- Повертає: `Driver` з id=12

#### 📥 Зчитати лише мета (напр. для форми створення)
```http
GET /api/drivers/meta/
```
- Повертає: `fields[]` з типами, назвами, reference-полями

#### 📤 Записати новий обʼєкт
```http
POST /api/drivers/
```

#### 📤 Змінити обʼєкт
```http
PUT /api/drivers/12/
```

---

### 📌 Особливості:

- `code` (наприклад, `drivers`, `countries`) — це ключ із `MODEL_MAP`
- `field_titles.py` забезпечує переклад назв полів (`ua`, `en`)
- Усі foreign key обробляються як `type: reference` із `ref: 'model_name'`
- API-інтерфейс фронтенда уніфіковано — усі довідники підʼєднуються автоматично
- Не потрібні окремі ViewSet-и на кожен справочник

---

### 📁 Структура `core` після рефакторингу

```
core/
├── api/
│   ├── gpt_api.py          # інтеграція з GPT
│   └── masterdata.py       # універсальне API
├── models.py               # всі моделі довідників
├── serializers.py          # універсальні серіалізатори
├── field_titles.py         # мапа локалізованих назв полів
├── urls.py                 # маршрути API
├── views.py                # базові view або порожній
```

---

## 🧠 Як додати новий довідник

1. 🔧 Додай модель у `models.py`
2. 🔧 Додай серіалізатор у `serializers.py`
3. 🔧 Додай рядок у `MODEL_MAP` у `masterdata.py`:
```python
"drivers": ("core", "Driver", DriverSerializer),
```
4. 🔧 (опційно) Додай назви полів у `field_titles.py`
5. ✅ Готово! API `/api/<code>/...` працює автоматично

## 🖥️ Frontend: робота з універсальним API (React + Ant Design)

### 📦 Основні хуки:

| Хук | Призначення |
|-----|-------------|
| `useMasterdataFull(code)` | Завантажує одночасно meta + data для списку |
| `useMasterdataItem(code, id)` | Завантажує meta + один запис для редагування |

---

### 🧩 Компоненти:

| Компонент | Призначення |
|-----------|-------------|
| `MasterdataSheet.jsx` | Відображення довідника у вигляді таблиці |
| `MasterdataForm.jsx` | Форма редагування/додавання елементів |
| `DynamicField.jsx` | Рендер поля форми на основі `type` і `meta` |
| `useMasterdataColumns.jsx` | Генерація колонок для таблиці на основі `meta` |

---

### ⚙️ Приклад використання:

#### 📄 Відкриття довідника:
```jsx
const { meta, data, isLoading } = useMasterdataFull("drivers");
```

#### ✏️ Редагування елемента:
```jsx
const { meta, data, loading } = useMasterdataItem("drivers", 12);
```

#### 🧩 Відображення поля:
```jsx
<DynamicField field={field} language={language} />
```

---

### 💡 Усі компоненти підтримують локалізацію через `field.title.ua/en`, адаптуються до `type`, відображають `reference` поля та службові іконки (`ismark`, `isfolder`).

📦 Додано модуль logistic — Логістика (травень 2025)
Ціль: побудувати процес-помічник для логіста — від створення контракту до формування маршрутного листа й план-факт аналізу доставки.

✅ Структура моделей:
LogisticOrder — стратегічна логістична задача, типу distribution (розвезення) або collection (збір).

LogisticLeg — базова одиниця перевезення: одна доставка одного товару з точки A до точки B певним видом транспорту.

Враховано поля status, planned_date, vehicle, cost_estimate, actual_date, quantity, transport_type.

🔌 Інтеграція з універсальним API:
Серіалізатори: LogisticOrderSerializer, LogisticLegSerializer

Вʼюхи: LogisticOrderViewSet, LogisticLegViewSet

Реєстрація через register_model() у apps.py з типом logisticOrders, logisticLegs

API підтримує GET /api/logisticOrders/, /meta/, /full/ — сумісно з Masterdata UI

📂 Структура додатку logistic/
models.py — 2 моделі

serializers.py — DRF-серіалізація

views.py — ViewSet-и

apps.py — реєстрація в системі

🧱 Пов’язані моделі (із core):
Contract, Organization — перенесені в core.models із підтримкою uuid, ismark, parent, __str__

🛠 Статус:
Модуль додано до INSTALLED_APPS як logistic.apps.LogisticConfig

Проведено makemigrations і migrate
# Інструкція з організації довідників (masterdata) в ESWF

## 1. Архітектура masterdata API

- Всі довідники (masterdata) у системі підключаються через універсальний masterdata API.
- Довідник (наприклад, одиниці виміру, банки, статті витрат) стає доступним на API, якщо:
    - Модель додана у models.py відповідного додатку (core, fleet тощо)
    - Довідник зареєстрований у apps.py через `register_model`
    - За потреби додані локалізовані заголовки у field_titles.py

---

## 2. Порядок додавання нового довідника (masterdata)

### 2.1. Створити модель

- Додай модель у відповідний models.py (наприклад, core/models.py)
    - Назви поля англійською (snake_case)
    - Додавай uuid, ismark, isfolder, parent — якщо потрібно

### 2.2. Зареєструвати у apps.py

- Додай модель у імпорт у CoreConfig.ready()
- Для простих моделей використовуй фабрику серіалізаторів:
    ```python
    from core.serializers_factory import generate_serializer
    register_model("units", "core", "Unit", generate_serializer(Unit))
    ```
- Для кастомних — підключай свій Serializer

### 2.3. Додати заголовки у field_titles.py (опціонально)

- Додай блок для нового довідника:
    ```python
    "units": {
      "code": {"ua": "Код", "en": "Code"},
      "name": {"ua": "Назва", "en": "Name"},
      ...
    }
    ```
- Якщо блок не додано — для назв полів у фронті використовується verbose_name з моделі

### 2.4. Міграції

- Зроби міграції:
    ```
    python manage.py makemigrations
    python manage.py migrate
    ```

### 2.5. Перевір API

- Для нового довідника одразу працюють:
    - `/api/meta/<code>/`
    - `/api/<code>/`
    - `/api/full/<code>/`
- Весь masterdata API автоматично підтримує новий довідник

---

## 3. Приклад повного циклу додавання довідника “Одиниці виміру” (units)

1. **Створити модель Unit у core/models.py**
    ```python
    class Unit(models.Model):
        uuid = models.CharField(max_length=36, default=uuid_lib.uuid4, editable=False, unique=True)
        code = models.CharField(max_length=10, unique=True)
        name = models.CharField(max_length=50)
        symbol = models.CharField(max_length=10, blank=True, null=True)
        comment = models.TextField(blank=True, null=True)
        ismark = models.BooleanField(default=False)
        def __str__(self):
            return self.name
    ```
2. **Зареєструвати у core/apps.py**
    ```python
    from core.serializers_factory import generate_serializer
    register_model("units", "core", "Unit", generate_serializer(Unit))
    ```
3. **Додати заголовки у field_titles.py**
    ```python
    "units": {
      "code": {"ua": "Код", "en": "Code"},
      "name": {"ua": "Назва", "en": "Name"},
      "symbol": {"ua": "Символ", "en": "Symbol"},
      "comment": {"ua": "Коментар", "en": "Comment"},
      "ismark": {"ua": "Позначено на видалення", "en": "Ismark"},
      "uuid": {"ua": "UUID", "en": "Uuid"}
    }
    ```
4. **Виконати міграції**
    ```
    python manage.py makemigrations
    python manage.py migrate
    ```
5. **Перевірити API**
    - `/api/meta/units/`
    - `/api/units/`
    - `/api/full/units/`

---

## 4. Додаткові примітки

- Якщо треба особливу поведінку (кастомна валідація, додаткові поля, read-only тощо) — створи окремий клас Serializer, зареєструй його вручну.
- Для реєстрації великої кількості masterdata можеш використовувати цикл та список моделей.

---

## 5. Структура файлів masterdata

- `models.py` — моделі довідників
- `apps.py` — реєстрація через register_model
- `serializers_factory.py` — фабрика серіалізаторів
- `field_titles.py` — локалізація назв полів
- `masterdata.py` — універсальний masterdata API (змінювати не потрібно)
---

## 6. Що робити далі

- Для кожного нового masterdata-довідника повторювати кроки 2.1–2.5.
- Для Transactional data та Registry data можна впровадити аналогічну систему.
- За потреби — автоматизуй генерацію field_titles через verbose_name з models.py.

---

**Твій masterdata API повністю автоматизований для додавання нових довідників та їх перекладів!**
